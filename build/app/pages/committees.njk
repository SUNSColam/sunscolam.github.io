{% extends "layout.njk" %}

{% macro card(title, url, img) %}
<div class="col-lg-4 col-md-6">
    <a href="./committee/{{url}}.html">
        <div class="speaker">
            <img src="./img/com-logos/{{img}}.jpg" alt="Speaker 1" class="img-fluid img-committees"/>
            <div class="details">
                <h3>
                    <a href="./committee/{{url}}.html">{{title}}</a>
                </h3>
            </div>
        </div>
    </a >
</div>
{% endmacro %}

{% block content %}
    {% import "partials/nav.njk" as nav %}
    {{nav.navigation(false)}}
    <main id="main" class="main-page">

        <!--========================== Speaker Details Section ============================-->
        <section id="speakers" class="wow">
            <div class="container">
                <div class="section-header">
                    <h2>Committees</h2>
                </div>

                {# TODO: Search bar #}

                {# <div class="container text-center">
                    <div class="p-1 bg-light rounded rounded-pill shadow-sm mb-4">
                        <div class="input-group">
                            <input type="search" placeholder="Search your committee" aria-describedby="button-addon1" class="form-control border-0 bg-light">
                            <div class="input-group-append">
                                <button id="button-addon1" type="submit" class="btn btn-link text-primary">
                                    <i class="fa fa-search"></i>
                                </button>
                            </div>
                        </div>
                    </div>
                </div> #}

                <div class="row">
                    {{ card('General Assembly (HS)', 'general-assembly-hs', 'ga') }}
                    {{ card('General Assembly (MS)', 'general-assembly-ms', 'ga') }}
                    {{ card('Security Council (HS)', 'security-council-hs', 'sc') }}
                    {{ card('Security Council (MS)', 'security-council-ms', 'sc') }}
                    {{ card('UNICEF (HS)', 'unicef-hs', 'unicef') }}
                    {{ card('UNICEF (MS)', 'unicef-ms', 'unicef') }}
                    {{ card('WHO (HS)', 'who-hs', 'who') }}
                    {{ card('WHO (MS)', 'who-ms', 'who') }}
                    {{ card('UN Women (HS)', 'un-women-hs', 'un-women') }}
                    {{ card('UN Women (MS)', 'un-women-ms', 'un-women') }}
                    {{ card('OHCHR (HS)', 'ohchr-hs', 'ohchr') }}
                    {{ card('OHCHR (MS)', 'ohchr-ms', 'ohchr') }}
                    {{ card('Arab League', 'arab-league', 'al') }}
                    {{ card('European Council', 'european-council', 'ec') }}
                    {{ card('ECOSOC', 'ecosoc', 'ecosoc') }}
                    {{ card('UNHCR', 'unhcr', 'unhcr') }}
                    {{ card('UNDP', 'undp', 'undp') }}
                    {{ card('UNEA', 'unea', 'unea') }}
                    {{ card('Mexican Cabinet', 'mexican-cabinet', 'mc') }}
                    {{ card('US Congress', 'us-congress', 'usc') }}
                    {{ card('CCPCJ', 'ccpcj', 'ccpcj') }}
                    {{ card('UNESCO', 'unesco', 'unesco') }}
                    {{ card('OAS', 'oas', 'oas') }}
                    {{ card('UNODC', 'unodc', 'unodc') }}
                </div>
            </div>
        </section>

    </main>
{% endblock content %}